<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ProjetDevMobile.Views.Nouveau"
             Title="{Binding Title}">

    <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <Image Source="{Binding SourceImage}"/>
        <Button Text="Take Photo" Command="{Binding CommandPrendrePhoto}" IsEnabled="{Binding ModeNouveau}"/>
        <Label Text="{Binding HeurePhoto}"/>
        <Entry Text="{Binding Nom}"/>
        <Entry Text="{Binding Description}"/>
        <Picker ItemsSource="{Binding ListeTags}" Title="Tag" SelectedItem="{Binding SelectedTag}" IsEnabled="{Binding ModeNouveau}"/>
        <Label Text="{Binding Position}"/>
        <Label Text="{Binding Adresse}"/>
        <Button Text="Save" Command="{Binding CommandAjoutEnregistrement}"/>
    </StackLayout>
  
</ContentPage>